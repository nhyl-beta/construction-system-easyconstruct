// Document oversight is identical to the Project Manager's document
// repository (the underlying feature module isn't role-specific) — reused
// directly instead of duplicating the page.
export { default } from "@/pages/roles/project-manager/pm-documents";
